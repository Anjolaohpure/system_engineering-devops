#!/usr/bin/env bash
# This script displays a list of currently running processes in a user-oriented format, # showing all processes for all users, including those without a TTY,
# and displaying the process hierarchy.

# 'ps auxf' provides:
# a: show all processes
# u: user-oriented format
# x: show processes without a controlling tty
# f: ASCII art process hierarchy (forest format)

echo "Displaying all running processes with hierarchy (user-oriented format):"
ps auxf
